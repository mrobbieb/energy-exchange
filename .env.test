# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'
APP_ENV=test
APP_DEBUG=0
DATABASE_URL="sqlite:///%kernel.project_dir%/var/app-test.db"

APP_DEBUG=0
DEFAULT_URI=http://localhost

# Use a dedicated test database file
# Lexik JWT: keep paths consistent with your app config
JWT_SECRET_KEY=%kernel.project_dir%/config/jwt/private.pem
JWT_PUBLIC_KEY=%kernel.project_dir%/config/jwt/public.pem

# IMPORTANT:
# Don't commit your real passphrase. In CI we’ll inject it from GitHub Secrets.
JWT_PASSPHRASE=change-me-in-ci